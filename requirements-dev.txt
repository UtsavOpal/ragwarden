# Fallback for toolchains without PEP 735 dependency-group support (pip < 25.1).
# Prefer: pip install --group dev
-e .
pytest>=8.0
pytest-cov>=5.0
ruff>=0.6
mypy>=1.11
types-PyYAML>=6.0
mkdocs-material>=9.5
mkdocstrings[python]>=0.26
